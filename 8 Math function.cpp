#include <iomanip>
#include <cmath>

#include <iostream>
#include <valarray>

//Math function :


int main() {
    double x=3.9;
    double y=4;
    double z=0;

    z= std::max(x,y);
    z= std::min(x,y);
    z= std::pow(y,z);
    z= std::sqrt(10);
    z=std::abs(y);


    return 0;
}
